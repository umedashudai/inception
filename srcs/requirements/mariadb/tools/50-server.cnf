[mysqld]

user            = mysql
pid-file        = /run/mysqld/mysqld.pid
socket          = /run/mysql/mysqld.socket
port            = 3306
basedir         = /user
datadir         = /var/lib/mysql
tmpdir          = /tmp
lc-messages-dir = /usr/share/mysql
bind-adress     = 0.0.0.0